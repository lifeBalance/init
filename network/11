From the output of `traceroute who.int` we can deduce that the **first hop** is the **IP** of the device that links our local network and the outside world. This is known as the **gateway**, and it enable instances in a private subnet to connect to the internet. We can confirm that checking the **routing tables** using `netstat -rn` or `route get who.int`.